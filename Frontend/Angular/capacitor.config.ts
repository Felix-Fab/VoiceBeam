import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'tk.felixfab.angular',
  appName: 'voice-beam',
  webDir: 'dist/voice-beam',
  bundledWebRuntime: false
};

export default config;
